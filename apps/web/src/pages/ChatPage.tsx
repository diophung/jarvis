import { EmptyState } from '../components/ui.js';

/** Placeholder — replaced by the Chat page implementation. */
export function ChatPage() {
  return (
    <div className="max-w-3xl mx-auto px-6 py-8">
      <EmptyState title="Chat — under construction" description="This page is being generated." />
    </div>
  );
}
