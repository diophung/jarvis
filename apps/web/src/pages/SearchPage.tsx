import { EmptyState } from '../components/ui.js';

/** Placeholder — replaced by the Search page implementation. */
export function SearchPage() {
  return (
    <div className="max-w-3xl mx-auto px-6 py-8">
      <EmptyState title="Search — under construction" description="This page is being generated." />
    </div>
  );
}
