import { EmptyState } from '../components/ui.js';

/** Placeholder — replaced by the Files page implementation. */
export function FilesPage() {
  return (
    <div className="max-w-3xl mx-auto px-6 py-8">
      <EmptyState title="Files — under construction" description="This page is being generated." />
    </div>
  );
}
