import { EmptyState } from '../components/ui.js';

/** Placeholder — replaced by the Approvals page implementation. */
export function ApprovalsPage() {
  return (
    <div className="max-w-3xl mx-auto px-6 py-8">
      <EmptyState title="Approvals — under construction" description="This page is being generated." />
    </div>
  );
}
