import { EmptyState } from '../components/ui.js';

/** Placeholder — replaced by the Audit page implementation. */
export function AuditPage() {
  return (
    <div className="max-w-3xl mx-auto px-6 py-8">
      <EmptyState title="Audit — under construction" description="This page is being generated." />
    </div>
  );
}
