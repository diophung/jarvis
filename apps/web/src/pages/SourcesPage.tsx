import { EmptyState } from '../components/ui.js';

/** Placeholder — replaced by the Sources page implementation. */
export function SourcesPage() {
  return (
    <div className="max-w-3xl mx-auto px-6 py-8">
      <EmptyState title="Sources — under construction" description="This page is being generated." />
    </div>
  );
}
