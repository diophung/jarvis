import { EmptyState } from '../components/ui.js';

/** Placeholder — replaced by the Digests page implementation. */
export function DigestsPage() {
  return (
    <div className="max-w-3xl mx-auto px-6 py-8">
      <EmptyState title="Digests — under construction" description="This page is being generated." />
    </div>
  );
}
