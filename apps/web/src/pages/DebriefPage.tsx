import { EmptyState } from '../components/ui.js';

/** Placeholder — replaced by the Debrief page implementation. */
export function DebriefPage() {
  return (
    <div className="max-w-3xl mx-auto px-6 py-8">
      <EmptyState title="Debrief — under construction" description="This page is being generated." />
    </div>
  );
}
